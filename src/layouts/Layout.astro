---
const { frontmatter } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={frontmatter.description}>
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href=`${import.meta.env.BASE_URL}/favicon.svg` />
		<title>{frontmatter.title}</title>
	</head>
	<body>
		<slot />
	</body>
    <style is:global>
        html {
            border-bottom: 150px #DA542D solid;
            border-top: 15px #DA542D solid;
            font-family: sans-serif;
            line-height: 1.5rem;
            padding: 2rem;
        }

        body {
            margin: auto;
            max-width: 45rem;
        }

        h1 {
            font-weight: 300;
        }

        h2 {
            font-weight: 300;
        }

        a {
            color: #DA542D;
        }
    </style>
</html>